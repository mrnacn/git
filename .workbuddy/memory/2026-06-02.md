# 2026-06-02

## 企业信用详情页：4个信息区加入"异议申请"按钮（2026-06-02 16:15）

### 改动文件
- `index.html`

### 具体修改
1. **CSS 新增** `.detail-section-header`（flex 布局，下划线分隔）+ `.objection-inline-btn`（红色边框按钮，hover 变红色实心）
2. **修改 `.detail-card h3`**：保留默认下划线样式给基础信息区，`.detail-section-header h3` 覆盖为无下划线
3. **修改 z-index**：`.modal-overlay` 从 2000 → 4000，确保异议弹窗显示在详情页之上
4. **JS：`showObjectionModal(companyName, companyCode)`**：新增参数，自动填充企业名称(`#objCompany`)和经营代码(`#objCompanyCode`)
5. **JS：新增 `openObjectionFromDetail(name, code)`**：从详情页调用的桥接函数
6. **HTML：4个 section 标题**（行政许可、行政处罚、守信激励、失信惩戒）改为 `.detail-section-header` 包裹，右侧加入红色"异议申请"链接

### 交互流程
点击详情页任意"异议申请" → 打开 `#objectionModal`（信用异议申诉申请弹窗）→ 企业名称和经营代码自动填充 → 用户填写其余信息后提交

## 信用公示-行政许可：删除状态列+企业名称/信用代码可点击进入详情（2026-06-02 16:08）

### 改动文件
- `index.html`

### 具体修改
1. **删除"状态"列**：表头删除 `<th>状态</th>`，4行数据的 `<td><span class="badge...">...` 全部移除
2. **可点击链接**：企业名称和统一社会信用代码改为 `<a class="report-link">` 链接，onclick 调用 `openEnterpriseDetail(企业名, 信用代码)`
3. 点击后打开已有的 `#enterpriseDetailOverlay` 详情页，展示该企业全部行政许可、行政处罚、守信激励、失信惩戒等信息（复用已有的 `openEnterpriseDetail` 函数和模板数据生成逻辑）

## 信用服务-专用信用报告弹窗改造

### 需求
1. 【申请本企业报告】弹窗：参考"弹窗-本企业专用报告.png"，企业名称+统一社会信用代码自动填入，新增联系人姓名/手机号/邮箱，提交前校验不完整提示"请完善填报信息"
2. 【查询其他企业-申请授权】弹窗：参考"弹框-企业专用报告.png"，新增申请事由、授权企业法人姓名/手机号、联系人姓名/手机号/邮箱，提交前校验不完整提示"请完善填报信息"

### 改动文件
- `index.html`

### 具体修改
1. **HTML结构**：原有 `#specialApplyModal` 改造为"申请本企业专用信用报告"弹窗，新增 `#specialAuthModal` 作为"申请查询企业专用信用报告"弹窗
2. **表单字段**：
   - 本企业报告：企业名称/信用代码(只读) + 报告类型/用途/时间/领域 + 联系人姓名/手机号/邮箱
   - 其他企业授权：被查询企业信息(只读) + 申请事由 + 授权企业法人姓名/手机号 + 联系人姓名/手机号/邮箱
3. **JS逻辑**：
   - `openSpecialApplyModal` 更新为使用联系人字段ID
   - `submitSpecialApply` 整体校验 + 失败显示spOwnValidTip
   - 新增 `openAuthModal`、`hideAuthModal`、`submitAuthApply` 三个函数
   - 搜索结果"申请授权"按钮改为调 `openAuthModal`
4. **CSS**：新增 `.sp-section-label` 样式（带红色左边框的分区标题）

### 更新：授权弹窗对齐本企业报告弹窗
- `#specialAuthModal` 增加了与 `#specialApplyModal` 一致的报告配置区：报告类型、报告用途、核查时间范围、核查领域（含全选/取消）
- 在报告配置基础上保留：申请事由(textarea)、授权企业法人姓名/手机号
- 新增 `renderAuthFieldCheckboxes()` 和 `toggleAllAuthFields()` 函数
- `openAuthModal` 增加单选重置和核查领域渲染
- `submitAuthApply` 增加核查领域校验和报告参数输出

### 更新：核查时间范围从选择改为只读显示
- 两个弹窗的"核查时间范围"区域从 radio 单选改为只读显示
- 显示内容：核查起始时间（报告申请日-3年）、核查结束时间（报告申请日-28天）
- 底部说明：行政处罚核查范围为"在公示"，严重失信主体名单核查范围为"在公示"
- 新增 `updateTimeDisplay(startId, endId)` 函数，在打开弹窗时自动计算并填入
- 移除 `spTimeRange` / `authTimeRange` 相关 radio 引用和 JS 残留代码
- CSS：新增 `.sp-time-display`、`.sp-time-row`、`.sp-time-label`、`.sp-time-value`、`.sp-time-desc` 样式

### 更新：实时拦截记录时间格式 + 查看更多（第二次实现，最终版 2026-06-02 12:45）

- 时间格式改为 `YYYY年MM月DD日 HH:MM:SS`（中文年月日格式）
- 静态HTML渲染3条实时拦截记录
- 新增"查看更多拦截记录 >"链接，位于表格下方
- 新增 `#historyBlockModal` 弹窗（宽950px），展示全部历史拦截记录
- 弹窗含筛选：风险等级下拉、处理状态下拉、关键词搜索输入框
- 新增 `historyBlockRecords` 数据数组（20条，跨越 2026-05-26 至 2026-06-02）
- 新增 `showHistoryBlockModal()` 打开弹窗并重置筛选
- 新增 `renderHistoryBlockTable()` 渲染筛选后表格
- 弹窗复用已有 `showModal`/`hideModal` + 遮罩点击关闭机制

### 更新：新闻管理功能全面改造（2026-06-02 14:49）

**改动文件**：`index.html`

**1. 数据模型升级**
- `ucNewsArticles` 每个条目新增字段：`cover`（封面图）、`summary`（摘要）、`content`（富文本HTML正文）、`attachments`（附件数组）
- 5条示例数据均填充了完整的摘要、正文和模拟附件

**2. 新建稿件 → 富文本编辑器**
- 正文区从 `<textarea>` 改为 `contenteditable` div + 富文本工具栏
- 工具栏支持：加粗、斜体、下划线、无序/有序列表、插入图片、插入链接
- 新增附件上传区（模拟文件选择 + 附件列表管理）
- 底部新增可折叠「内容预览」面板，实时渲染标题/来源/封面/摘要/正文/附件

**3. 新闻详情页**
- 新增 `viewArticle(id)` → `renderArticleDetail(article)` 函数
- 详情页包含：分类标签、标题、来源/时间/发布状态元信息、封面图、摘要、正文、附件列表
- 底部有「返回列表」「修改」「提交审核」等快捷操作按钮
- 草稿/已驳回状态提供快捷修改入口

**4. 修改/提交流程**
- 列表中「修改」→ 调用 `editArticle(id)`，复用新建稿件表单并预填数据
- 列表中「提交」→ 调用 `submitDraftArticle(id)`，弹出确认框后状态从「草稿」→「待审核」
- 「保存草稿」支持更新已有草稿（带 existingId 参数）
- 新增 `deleteArticle(id)` 删除确认

**5. 上架/下架切换**
- 列表中「发布状态」列和操作列「上架/下架」均可点击
- 点击触发 `togglePubStatus(id)`：
  - 上架 → confirm"更改为下架" → 确认后变下架
  - 下架 → confirm"更改为上架" → 确认后变上架
- 状态变更后自动刷新列表

**6. 新增CSS**：富文本工具栏、编辑器、附件区、预览面板、文章详情页全套样式
**7. 新增JS函数**：`viewArticle`、`editArticle`、`submitDraftArticle`、`deleteArticle`、`togglePubStatus`、`getArticleFormData`、`execRichCmd`、`insertImageAtCursor`、`insertLinkAtCursor`、`simulateAddAttachment`、`handleAttachmentAdd`、`removeAttachment`、`renderAttachmentList`、`formatFileSize`、`togglePreview`、`updatePreview` 等

### 更新：历史拦截记录弹窗改为独立页面（2026-06-02 15:02）

**改动文件**：`index.html`

**1. HTML 改造**
- 删除 `#historyBlockModal` 弹窗，新建 `#page-historyblocks` page-section 独立页面
- 页面包含：面包屑导航（首页 > 意识形态审查 > 历史拦截记录）、标题、"返回实时审查"按钮
- 筛选区新增起始时间/结束时间两个 `<input type="date">` 日期选择器
- 新增"重置筛选"按钮，一键清空所有筛选条件

**2. 数据升级**
- `historyBlockRecords` 每条记录新增 `dateKey` 字段（YYYY-MM-DD 格式），用于日期范围过滤

**3. JS 函数改造**
- `showHistoryBlockModal()` → 替换为 `navigateToHistoryBlocks()`：隐藏所有 page-section + 取消导航高亮 + 激活 `#page-historyblocks`
- 新增 `backToRealtimeReview()`：调用 `navigateTo('audit')` 返回实时审查
- 新增 `resetHistoryBlockFilters()`：重置等级、状态、日期、搜索关键词并重新渲染表格
- `renderHistoryBlockTable()` 新增 `dateKey >= start` 和 `dateKey <= end` 的日期范围过滤逻辑

**4. "查看更多拦截记录"链接**
- 从 `showHistoryBlockModal()` 改为 `navigateToHistoryBlocks()`

### 更新：意识形态审查 - L4+审核功能 + 驳回原因展示（2026-06-02 15:47）

**改动文件**：`index.html`

**需求1：L4+待审核→审核→通过/驳回**
- `renderReviewRecords()` 修改：L4+ pending状态的"✓ 通过"/"✗ 驳回"链接改为"审核"按钮，点击调用 `openIdeologyReview(id, 'review')`
- `#ideologyReviewModal` 弹窗新增 `#ideologyReviewActions` 审核操作区（默认隐藏）：含"✓ 通过"按钮 + "✗ 驳回"按钮 + 驳回原因textarea（`#ideologyRejectReasonBox`，默认隐藏）
- `openIdeologyReview` 新增 `mode` 参数，`mode === 'review'` 时显示审核操作区、标题改为"内容审核"
- 新增 `ideologyApprove()`：将记录status改为published、opLog追加"审核通过"条目、关闭弹窗刷新列表
- 新增 `ideologyReject()`：首次点击展开驳回原因输入框（按钮变为"确认驳回"），二次点击校验+执行驳回（更新status/opLog/rejectReason）
- CSS新增：`.ideology-review-actions`、`.btn-success`/`.btn-danger` 审核按钮样式、`.ideology-reject-reason` textarea样式

**需求2：已驳回记录→查看→操作日志展示驳回原因**
- `openIdeologyReview` 中操作日志render增加 `reject-item` class，驳回条目红色高亮（红色节点+红色文字）
- CSS新增：`.ideology-tl-item.reject-item` 驳回条目样式（文字红色、时间轴节点红色）
- R-20260521-004 已有驳回记录新增 `rejectReason` 字段
- 新驳回操作会在opLog中写入 `驳回稿件，原因：XXX`
- 弹窗标题按模式动态切换："内容审核" / "内容详情"
- 打开弹窗时自动重置驳回按钮文案为"✗ 驳回"

### 更新：意识形态审查 - L3用户内容审核记录"查看"功能（2026-06-02 15:26）

**改动文件**：`index.html`

**1. CSS 新增**（约40条样式规则）
- `.highlight-high` / `.highlight-medium` / `.highlight-low`：风险内容高亮样式，红色(#fca5a5)/黄色(#fde68a)/绿色(#bbf7d0)背景，hover时显示风险等级tooltip
- `.ideology-risk-legend`：风险图例条（高/中/低风险色块说明）
- `.ideology-timeline` + `.ideology-tl-item`：竖线时间轴样式，用于操作日志

**2. HTML 新增**
- `#ideologyReviewModal` 弹窗（宽1000px），含标题栏、内容区、关闭按钮

**3. 数据升级**
- `reviewRecords` 8条记录全部扩展：新增 `content`（正文）、`highlights`（风险高亮标注数组，含text/level/rule）、`opLog`（操作日志数组，含time/user/action/type）
- 其中R-002/R-006无风险内容、R-001/R-005无风险内容、R-003/R-006/R-008含中风险、R-004含高风险
- 操作日志覆盖：创建→检测→审核→发布/驳回全流程

**4. JS 函数**
- `renderReviewRecords()` 修改：R05(L3)的actions列新增"查看"链接（调用`openIdeologyReview`）；L4+在非pending状态也新增"查看"
- 新增 `openIdeologyReview(id)`：打开弹窗，渲染元数据 + 风险图例 + 高亮正文 + 操作日志时间轴
- 新增 `renderHighlightedContent(content, highlights)`：用占位符法安全替换风险文本为高亮span，防短文本被长文本误匹配
- 新增 `closeIdeologyReviewModal()`：调用 `hideModal` 关闭

**5. 交互逻辑**
- 点击遮罩层自动关闭（复用已有 `.modal-overlay` click handler）
- 操作日志按时间顺序排列，每条带图标（📝创建/🔍检测/✅审核/❌驳回/📤发布/📋提交）
