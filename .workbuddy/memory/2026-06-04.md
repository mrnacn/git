# 2026-06-04 工作日志

## 首页政策法规增加分类Tab
- `.np-card` 标题右侧新增3个 `.np-tab`：国家级 / 湖北省级 / 武汉市级
- 三套独立的政策法规列表（`#hplist-national` / `#hplist-province` / `#hplist-city`），默认显示国家级
- 使用 document click 事件委托处理 Tab 切换，避免 DOMContentLoaded 时序问题

## 首页信用动态Tab交互
- Tab 增加 `data-hdtab` 属性（wuhan / hubei / national）
- 新增湖北、国家两套独立新闻列表（`#hdlist-hubei` / `#hdlist-national`）
- 点击Tab切换对应内容，默认显示武汉

## 首页核心服务与信用服务页关联
- 三个 service-card 改为可点击跳转（`onclick="goToService('report')"` 等）
- "信用修复申请"卡片改为"信用异议申请"（与信用服务页模块对应）
- 新增 `goToService(sub)` 函数：先调用 `navigateTo('service')` 切换页面，再延迟50ms调用 `navigateToServiceSub(sub)` 定位到对应子模块

## 信用动态页大家关注→诚信宣传
- `#dynamicTabs` 中 `data-dtab="focus"` 的 Tab 文字改为"诚信宣传"
- `newsCategoryOptions` 中对应选项同步更新

## 信用承诺公示搜索框下移
- `#ptab-chengnuo` 内容区结构调整：分类 subtab（chengnuo-subtabs）移到搜索框（pub-search-bar）上方
- CSS 间距不变，.chengnuo-subtabs 的 margin-bottom 保持 16px

## 数据统计页武汉各区真实地图热力图
- 完全重构 SVG 地图：按真实武汉地理布局绘制 13 区多边形，长江 SW→NE 横穿、汉江 W→E 汇入
- 各区 polygon 颜色按数据量 5 级渐变：武昌(1280,#8b0000)→洪山(1156,#b71c1c)→江汉(980,#c41230)→江岸(892,#d32f2f)→东西湖(765,#e53935)→硚口(680,#e74c3c)→汉阳(612,#ef5350)→青山(545,#f1948a)→江夏(498,#f5b7b1)→黄陂(432,#f8cecc)→蔡甸(356,#fadbd8)→新洲(298,#fde8e8)→汉南(189,#fef2f4)
- 新增 `#mapTooltip` 悬浮提示：hover 到地图任意区弹出黑色半透明气泡，显示区名+数据量
- 地图与右侧列表双向联动：hover 地图→高亮对应排行行（`.highlight-row` 粉色背景）；hover 列表→高亮对应地图区（`.highlight-poly` 黑色描边加粗）
- 右侧排行列表每行均加 `data-district` 属性用于 JS 匹配；前三名排名标红
- 图例保留：渐变条 `#fde8e8→#8b0000`，标注"低""高"

## 专项信用报告页面新增申请流程图
- 在四个功能入口卡片下方、查询区域上方新增 `.special-flow-section` 流程图区块
- 5 步水平流程：身份核验 → 提交申请 → 系统核查 → 生成报告 → 下载核验
- CSS 设计：渐变红色圆形步骤编号（52px 带阴影），箭头使用左侧渐变线 + 三角箭头
- 每个步骤 hover 上移 4px 动效，阴影加深
- 描述文案对齐页面术语：44个领域、近3年数据、约1分钟、PDF报告

## L4+用户中心新增"数据统计管理"功能
- 新增全局 `statisticsData` 对象：涵盖 4 个核心指标卡片、13 个区数据、3 种主体类型分布、6 个数据来源部门
- 每个数据项均带 `mode: 'auto'/'manual'` 属性，通过 toggle 开关切换
- 开关 UI：iOS 风格 slider（44×24px），红色主题，带"手动"/"接口"标签
- `applyStatisticsData()` 动态更新数据统计页面及首页统计卡片
- R07(L4) 和 R08(L5) 菜单新增"数据统计"项（icon:📈，id:stats-manage）
- `renderStatsManage()` 渲染管理面板：4个区块分别管理卡片/地图/类型分布/部门数据
- 切换至"手动"模式后，输入框启用，即时更新前端的【数据统计】页面显示

## 角色切换器移至右上角固定位置
- 角色切换器从 nav 栏内 `user-actions` 移至页面右上角固定 `.role-toolbar`
- 工具栏：`position:fixed;top:8px;right:12px;z-index:10001`，白色半透明背景 + backdrop-blur
- `.user-menu` 下拉菜单改为 `position:fixed`（z-index:10000），通过 JS 动态计算 `top/right`
- 新增 `toggleUserMenu(event)` 函数：基于 `userCenter` bounding rect 计算菜单位置
- 新增全局 click 监听：点击菜单外区域自动关闭
- `.main-nav` 和 `.main-nav .container` 设 `overflow:visible`，防止下拉被裁剪
- `.hero-slider` 设 `z-index:1`，确保轮播图在导航栏之下
